# ---- HTTP server ----
PORT=4000
HOST=localhost
# Allowed CORS origin for the frontend. Leave blank to reflect any origin (dev).
CORS_ORIGIN=

# ---- MongoDB ----
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=zerobug
